# 育てる地図 — 要件定義・技術設計

更新日: 2026-09-05\
用途: Codex Student Builder Fest 2026 のチーム設計・実装準備

## 1. プロダクト概要

「育てる地図」は、歩いた場所や体験をもとに自分と街の関係が蓄積され、他人の街での過ごし方を自分の街へ移し替え、必要な地図機能を自然言語から追加できる地図アプリである。

一般的な地図は、道路や施設の情報を持っていても、その人がどこを知り、何を好み、どの経路に慣れているかまでは持たない。本プロダクトでは、移動履歴、訪問、記憶、時間帯、行動の傾向などを個人ごとの空間情報として蓄積し、地図の表示、推薦、経路生成、友人との共有、防災情報との重ね合わせに利用する。

地図は最初から道路や建物の位置を表示する。未訪問地域を霧で隠すのではなく、歩いた場所ほど建物や経路が立体化し、記憶や意味が追加されることで「自分の街が立ち上がる」体験を作る。

## 2. プロダクトの中心となる体験

### 2.1 歩くほど街が立ち上がる

初期状態では道路、河川、建物の輪郭、地形、主要施設を簡略表示し、ユーザーが移動すると経路周辺の建物や街の要素を順に立体化する。

建物の高さは実際の高さを使い、熟知度は不透明度、詳細度、入口や階層、記憶、意味ラベルなどで表現する。建物の高さと「どれだけ知っているか」は混同しない。

駅や複合施設では、地上、地下、上層階を別々の場所として扱い、「地上はよく知っているが地下街は知らない」といった状態も保持できるようにする。

### 2.2 自分と街の関係を蓄積する

単なるGPSログではなく、次の情報をユーザーと場所の関係として保存する。

- 訪れた場所と経路
- 滞在時間、時間帯、曜日、訪問頻度
- よく使う地域や経路
- 場所に対する好みと、その条件
- 写真、メモ、会話から得た記憶
- 地域や経路への熟知度
- 行動の傾向
- 情報の根拠と公開範囲

「水辺を好む」「夜に散歩することが多い」などの推定結果は、根拠と一緒に表示し、本人が修正できるようにする。

### 2.3 他人の体験を別の街へ移し替える

共有するのは経路の座標そのものではなく、その経路がどのような体験だったかを表す特徴である。

たとえば「考え事をしたい夜の30分散歩」であれば、所要時間、静けさ、水辺、緑、混雑、屋内外、坂や階段、未知の場所を含む割合、終点の役割などへ分解し、別の都市で似た体験になる経路を生成する。

友人が名古屋で使っている散歩経路を横浜へ移す場合も、元の道路を再現するのではなく、「静かな道を通り、水辺を経由し、最後に一人で入りやすい店へ着く」といった体験の構造を保つ。

共有データには、生のGPS軌跡、自宅などの生活圏を推定しやすい情報、非公開の記憶を含めない。

### 2.4 欲しい地図機能を自然言語から追加する

ユーザーは「大雨のとき、浸水想定と現在の河川状況を、いつもの道に重ねたい」のように入力できる。

Codexは必要な公開情報源を調べ、データ取得処理、共通形式への変換、表示方法、権限、テストを作成し、プレビューを提示する。ユーザーが内容と利用データを確認した後に、その機能を地図へ追加する。

一度追加した機能の通常更新では毎回Codexを呼ばず、生成済みの処理を実行する。外部サイトの構造変更などで取得に失敗した場合だけCodexが修復案を作り、テストと差分確認を経て更新する。

## 3. 主要機能

### 3.1 地図表示

- 道路、河川、地形、建物輪郭、主要施設を初期状態から表示する。
- 移動またはデモ用の経路再生に合わせて、周辺の建物や経路を立体化する。
- 実際の建物高度を保持したまま、熟知度に応じて表示の詳細を増やす。
- 地上、地下、上層階を別の空間として扱えるようにする。
- 写真、メモ、記憶、地域知を場所や階層へ紐付ける。
- 個人情報、推薦結果、友人由来情報、災害情報など複数の表示を重ねられるようにする。

### 3.2 個人空間モデル

- 位置、移動、滞在、訪問頻度、時間帯を共通形式で記録する。
- 場所、地域、経路、記憶、行動傾向との関係を保持する。
- 場所への好みを、時間帯、天候、同行者、目的などの条件と一緒に扱う。
- 地域、経路、階層ごとに熟知度を計算し、その根拠を表示する。
- AIが推定した傾向を本人が修正できるようにする。
- 個々の情報に「自分のみ」「友人まで」「地域へ共有」の公開範囲を持たせる。

### 3.3 経路体験の転移

- 経路や説明文から、目的、時間、環境、移動条件、地点の役割などを抽出する。
- 別都市の道路網と施設情報から複数の候補経路を生成する。
- 対象ユーザーの好み、熟知度、現在地、移動制約を評価へ反映する。
- 「賑やかな場所から静かな場所へ移り、その後水辺を通る」といった順序も比較する。
- 結果には、元の体験から維持できた要素、変わった要素、未確認の要素を表示する。
- 静けさ、未知の場所の割合、距離、屋内外、階段などをユーザーが調整できるようにする。

### 3.4 地図機能の追加

追加できる機能は、任意のJavaScriptをブラウザで実行するプラグインではなく、許可された表示・評価・分析の組み合わせとして定義する。

主に次の処理を扱う。

- 地点、領域、経路の表示
- 場所の順位付け
- 経路の評価
- 分析結果のカード表示
- 時刻、天候、場所、イベントによる自動切り替え
- 外部公開データの取得と表示

追加前には、使用する個人情報、接続先、取得頻度、出典、必要な権限を表示する。定義やテストが不正な機能は追加できないようにする。

既存機能を複製して変更し、派生元と差分を残せるようにする。

### 3.5 外部データ取得

外部のAPI、CSV、GeoJSON、HTML、PDFなどを取得し、地図上で扱える共通形式へ変換する。

各データには最低限、位置、種類、値または状態、観測時刻、出典、情報の状態、原典への参照を持たせる。更新期限のある情報では、有効期間も保持する。

JSON API、CSV、GeoJSON、単純なHTMLなどは既存の標準処理で扱い、それでは対応できない複雑な解析だけ生成コードを隔離環境で実行する。

### 3.6 防災情報の重ね合わせ

防災は独立した防災アプリとして作るのではなく、日常的に育てた地図へ新しい情報を追加できることを示す代表例として実装する。

- 公式の洪水・浸水想定区域を表示する。
- 河川水位などの現在情報を表示する。
- 更新時刻と出典を必ず表示する。
- 自分の生活圏、普段の経路、熟知度とハザード区域の重なりを計算する。
- 避難場所などの公開情報も表示する。
- 未踏区間、古い情報、取得失敗、情報不足を明示する。

AIやCodexは避難経路の安全性を保証しない。情報が足りない場合は推測で埋めず、判断できないことをそのまま表示する。

### 3.7 友人・地域との共有

友人とは、経路体験の特徴、意図的に選択した場所、集約した熟知情報を共有できるようにする。

地域へ還元する情報は、生の位置履歴ではなく、ユーザーが確認した知見に限定する。「雨の日に歩きやすい」「夜は静か」「一人で入りやすい」などの情報には、条件、鮮度、確信度、公開範囲を持たせる。

他人から得た情報と、自分が実際に経験した情報は混同しない。

## 4. 画面構成

### ホーム地図

画面の大部分を地図に使い、操作は下部のシートへまとめる。現在地、経路再生、表示切り替え、記憶、友人由来情報などを地図上で扱う。

### 自分と街の関係

熟知している地域、よく使う道、好み、行動傾向、記憶を表示する。推定結果には根拠と修正操作を付ける。

### 経路体験の転移

元の体験をどの特徴へ分解したか、別都市でどの経路へ置き換えたか、何が維持され何が変わったかを一画面で比較する。

### 地図機能の作成

自然言語入力とCodexの処理状況を表示する。単なるチャット画面にはせず、情報源の発見、データ取得処理の作成、テスト、プレビューまでを地図の変化と同期して見せる。

### 権限・出典確認

新しい機能を追加する前に、利用する個人情報、外部接続先、取得頻度、出典、保存内容を一枚で確認できるようにする。

## 5. データ構造

内部実装では、画面上の名称とは別に安定したスキーマ名を使う。

| データ | 役割 |
|---|---|
| `location_event` | 位置、時刻、高度、精度などの移動イベント |
| `place` / `area` / `route` | 地点、地域、経路 |
| `experience` | 訪問や滞在などの体験 |
| `spatial_relation` | 訪問、熟知、好み、記憶、行動傾向の関係 |
| `familiarity` | 地域・経路・階層ごとの熟知度と内訳 |
| `memory` | 場所に紐付く写真やメモ |
| `route_profile` | 経路体験を別都市へ移すための特徴 |
| `route_translation` | 転移時の候補、評価、説明 |
| `map_feature` / `map_feature_version` | 追加した地図機能とその履歴 |
| `data_source` / `data_source_version` | 外部データ取得処理とその履歴 |
| `geo_record` | 外部情報を統一して保存する地理情報 |
| `community_record` | 匿名化・確認済みの地域知 |
| `permission_grant` | 個人データや外部接続の利用許可 |
| `demo_scenario` | デモの固定データ、カメラ、状態、再生手順 |

個人空間モデルは論理的にはグラフ構造だが、グラフデータベースを必須とはしない。

## 6. 技術構成

### 6.1 全体方針

地図描画、認証、データベース、経路探索、ジョブ管理、コード隔離といった一般的な基盤は既存技術を利用し、このプロダクト固有の体験だけを自作する。

採用候補は次の通り。

| 領域 | 採用技術 | 用途 |
|---|---|---|
| Webアプリ | Next.js + TypeScript | 画面と通常API |
| UI | Tailwind CSS + shadcn/ui + Motion | 画面部品とアニメーション |
| 地図 | MapLibre GL JS + react-map-gl | ベース地図、カメラ、通常レイヤー |
| 高度な地図表現 | deck.gl | 建物の立ち上がり、経路、3D表現 |
| 地図データ | PMTiles + OSM/Protomaps | 道路、水域、土地利用など |
| 3D都市データ | PLATEAU | 建物形状、高さ、必要地点の3Dデータ |
| バックエンド | Supabase | Postgres、PostGIS、認証、Storage、Realtime |
| 意味類似 | pgvector | 場所や経路の意味的な比較 |
| 経路候補生成 | openrouteservice | 歩行経路の候補生成 |
| 空間演算 | Turf.js | 距離、交差、バッファなど |
| 長時間処理 | Trigger.dev | 定期取得、再試行、検証 |
| 生成コード隔離 | E2B | 複雑な外部データ解析の実行 |
| Codex対話 | Codex App Server | 作成画面、承認、進捗表示 |
| Codex自動処理 | Codex SDK | 検証、修復、CI、定期処理 |
| テスト | Vitest + Playwright + MSW | 単体、E2E、外部データの再現 |
| 配信 | Vercel + Supabase + Railway + Cloudflare R2 | Web、DB、長寿命処理、静的データ |

### 6.2 地図と3D

地図エンジン自体は作らず、MapLibreを基本地図とカメラ制御に使い、deck.glで建物や経路の動的表現を重ねる。

デモ対象地域のPLATEAU建物データは事前に取得・加工し、GeoJSON、MVT、PMTilesなどで静的配信する。デモ中にPLATEAU APIへ毎回依存しない。

建物の立ち上がりは、実際の高さに0から1までの進行値を掛けて描画する。立ち上がるタイミングは経路からの距離や訪問時刻から決定し、同じデモを何度でも再現できるようにする。

CesiumJSは高精細な3D都市表示には強いが、今回はモバイル向けの地図UI、経路表示、個人情報との重ね合わせ、建物単位のアニメーションを一つの地図で制御する必要があるため、主地図には採用しない。

Re:Earth、TerriaJS、kepler.glなどの完成GISアプリも土台にはしない。既存の画面やデータ管理の思想を解体する必要が大きく、MapLibreやdeck.glのような低い層のライブラリだけを利用した方が早い。

### 6.3 バックエンド

SupabaseのPostgresを中心にし、PostGISで位置関係、pgvectorで意味類似を扱う。認証、Storage、RealtimeもSupabaseへまとめ、友人共有や個人データのアクセス制御はRLSで強制する。

個人空間モデルのためだけにNeo4jなどの別グラフデータベースは導入しない。主要な処理は、浅い関係探索、空間結合、時系列、意味類似であり、Postgresで十分に扱える。

### 6.4 経路体験の転移

処理は次の順に行う。

1. 経路や説明文から体験の特徴を構造化する。
2. PostGISで水辺、緑、施設、熟知度、垂直情報などを候補地域へ付与する。
3. openrouteserviceで複数の歩行経路を生成する。
4. Turf.jsで距離や交差などの補助計算を行う。
5. pgvectorで場所や経路の意味的な類似度を計算する。
6. 所要時間、静けさ、水辺、順序、未知率などを決定論的に評価する。
7. 評価結果をもとに、何を維持できたかを自然言語で説明する。

LLMに経路を直接選ばせず、特徴抽出と説明に使う。候補生成と順位付けは再実行可能な処理にする。

### 6.5 地図機能の生成と実行

追加する地図機能は、表示方法、評価方法、発火条件、必要権限、情報源、テストをJSONで定義し、許可された処理だけを実行する。

通常の地図表示にはMapLibreやdeck.glの既存レイヤーを使い、条件判定には副作用を持たないルール表現を使う。任意のReactコンポーネントやJavaScriptを直接実行させない。

外部データ取得は二段階に分ける。

**標準処理**では、JSON API、CSV、GeoJSON、単純なHTMLなどを既存ライブラリで取得・変換する。

**個別処理**では、標準処理で扱えないHTMLやPDFだけ、Codexが生成した解析コードをE2Bなどの隔離環境で実行する。

### 6.6 Codex

ユーザーが地図機能を作る場面ではCodex App Serverを使い、会話、進捗、承認、生成物をアプリ画面へ表示する。

通常のデータ更新ではCodexを呼ばず、生成済みの取得処理を実行する。取得失敗や構造変更を検知した場合はCodex SDKを使って修復候補を生成し、テスト結果と変更差分を確認してから反映する。

単位やデータの意味そのものが変わった場合は自動更新せず、承認待ちにする。

## 7. 既存技術を使う部分と自作する部分

| 対象 | 方針 | 自作する部分 |
|---|---|---|
| アプリ基盤・認証 | 既存基盤を利用 | 地図中心の画面構成とデモ用導線 |
| 地図描画 | MapLibreを利用 | 個人情報や追加機能の組み合わせ方 |
| 3D描画 | deck.glを利用 | 歩行に連動して街が立ち上がる制御 |
| 地図データ | OSM、PLATEAU等を利用 | デモ地域の前処理と表示状態 |
| DB・権限 | Supabaseを利用 | 個人空間モデル、熟知度、共有規則 |
| 経路探索 | openrouteserviceを利用 | 体験特徴への分解、候補評価、説明 |
| 外部データ解析 | 既存parserを利用 | 共通形式、生成・修復フロー |
| ジョブ管理 | Trigger.devを利用 | 失敗検知、修復、画面への進捗連携 |
| Sandbox | E2Bを利用 | 許可範囲と生成コードの実行契約 |
| Codex | App Server / SDKを利用 | 地図機能生成、テスト、修復の製品体験 |
| デモ | ライブラリを利用 | 固定再生、リセット、シーン制御 |

自作すべき価値は、地図エンジンや認証ではなく、次の五つに集中する。

1. 歩行に連動して街が立ち上がる体験
2. 自分と街の関係を蓄積するモデル
3. 経路体験を別の都市へ移し替える処理
4. 地図機能を自然言語から生成し、安全に追加する仕組み
5. 外部情報の取得処理をCodexがテスト・修復する仕組み

## 8. デモ

ライブデモは機能一覧を順に見せるのではなく、一つの流れで価値を説明する。

### Scene 1 — 街が立ち上がる

簡略表示された地図から経路再生を始め、移動に沿って周囲の建物、道、記憶を立ち上げる。

### Scene 2 — 自分と街の関係が見える

熟知している地域、好み、行動傾向とその根拠を表示し、一つの推定を修正する。

### Scene 3 — 友人の体験を別の街へ移す

友人の名古屋での夜散歩を特徴へ分解し、横浜で自分向けの経路へ再構成する。元の体験から何が維持されたかを比較表示する。

### Scene 4 — 新しい地図機能を作る

「大雨のとき、ハザードと現在の河川状況をいつもの道に重ねたい」と入力し、Codexが情報源の確認、取得処理の作成、テスト、プレビューまで進める。

### Scene 5 — 壊れた情報源を直す

外部サイトの構造変更を再現し、テスト失敗、差分解析、修復、再テスト、復旧を連続して見せる。

### Scene 6 — 日常地図を防災へ切り替える

普段の地図に浸水想定、河川情報、生活圏、いつもの経路、未踏区間、出典、更新時刻を重ねる。

## 9. デモの安定化

デモ用の位置履歴、経路候補、PLATEAUデータ、公開情報、外部サイトのHTMLなどは固定データとして保存し、ネットワークが不安定でも同じシナリオを再現できるようにする。

Live GPSと固定経路再生は同じイベント形式を使い、いつでも切り替えられるようにする。各シーンには個別の開始状態と復帰点を用意し、前のシーンが失敗しても次へ進めるようにする。

この仕組みは機能削減のためではなく、ライブデモで実装済みの機能を確実に見せるために用意する。

## 10. 本質的に実装しないもの

次の要素は時間不足ではなく、プロダクトの他の機能と衝突するため実装しない。

- 第三者が作った機能によるブラウザ上の任意JavaScript実行
- 友人の継続的な生GPS位置の標準共有
- AIによる避難経路の安全保証
- 利用条件を無視した任意サイトの無制限クロール
- 世界全域の高精細3Dデータの常時配信
- Webアプリでのバックグラウンド常時位置取得への依存

## 11. コンペ規約と事前準備

既存のOSS、公式スターター、公開データ、第三者サービスは、権利と利用条件を確認し、提出時に名称、バージョン、ライセンス、用途、改変の有無を開示する。

事前には、要件、データスキーマ、画面設計、プロンプト、AGENTS.md、Skills、テストケース、固定データ、各機能のPoCまで十分に検証する。当日はプロダクト固有の統合、画面、個人空間モデル、経路体験の転移、地図機能生成、Codex連携を公式開発時間内に構築し、Codexの実行履歴、テスト、コミットを残す。

既存のチーム資産を直接利用する場合は、事前資産として範囲、権利者、ライセンス、コミットを明示する。

## 12. 実装順序

実装順序は機能を削るための優先順位ではなく、複数人とCodexが並行して開発できるよう依存関係を切るために使う。

1. 地図基盤、PLATEAU表示、建物の立ち上がり、固定経路再生
2. Supabaseのスキーマ、PostGIS、個人空間モデル、権限制御
3. 経路体験の特徴形式、候補生成、評価、比較画面
4. 地図機能の定義形式、プレビュー、追加、権限確認
5. Codex App Serverとの接続、外部データ取得処理の生成、テスト
6. 情報源破損、修復、ロールバック、デモリセット
7. 地下・上層階、派生機能、地域知収集などの拡張

各工程では、次工程が待たずに作業できるよう、JSON SchemaまたはZodで入出力形式を先に固定する。

## 13. 最終的な設計方針

このプロダクトで一から作るべきなのは地図やバックエンド基盤ではない。

MapLibre、deck.gl、Supabase、PostGIS、openrouteservice、Trigger.dev、E2B、Codex App Serverなどを組み合わせ、既存技術では提供されない「自分と街の関係が育つ」「人の体験を別の街へ移せる」「必要な地図機能をCodexが実装し、壊れた後も修復できる」という部分へ開発時間を集中する。
