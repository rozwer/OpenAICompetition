# Optional: retain Codex default model unless explicitly set.
CODEX_MODEL=
# Optional path to a Codex executable for nonstandard installations.
CODEX_BINARY=
# Required only for demo login from another device; keep private.
DEMO_ACCESS_CODE=
# openrouteservice Standard API key; server only. Never use NEXT_PUBLIC_.
ORS_API_KEY=
